# SPDX-FileCopyrightText: 2026 TII (SSRC) and the Ghaf contributors
# SPDX-License-Identifier: Apache-2.0

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "keylime-measured-boot-policy"
version = "0.1.0"

[tool.setuptools]
py-modules = ["measured_boot_policy"]
